# 🤖 Indy7 3쿠션 당구 로봇

> **POSTECH 로보틱스개론 팀 프로젝트**
>
> Neuromeka Indy7 6-DOF 로봇 팔 + ㄴ자 도구로 3쿠션 당구를 자율 수행하는 시스템

---

## 📋 목차

- [프로젝트 개요](#-프로젝트-개요)
- [시스템 아키텍처](#-시스템-아키텍처)
- [디렉토리 구조](#-디렉토리-구조)
- [핵심 모듈](#-핵심-모듈)
- [설치 및 실행](#-설치-및-실행)
- [성능 및 최적화](#-성능-및-최적화)
- [왜 이 프로젝트는 어려운가](#-왜-이-프로젝트는-어려운가)
- [설계 결정에 대한 기술적 근거](#-설계-결정에-대한-기술적-근거)
- [포켓볼 데모 & POSTECH 트릭샷](#-포켓볼-데모--postech-트릭샷)

---

## 🎯 프로젝트 개요

### 목표
Indy7 로봇 팔에 ㄴ자형 타격 도구를 장착하고, **자율 상태 머신(SCAN → THINK → STRIKE → OBSERVE)**을 통해 3쿠션 당구를 수행합니다.

- 수구(흰공)로 황구·적구 모두 접촉
- 총 쿠션 반사 2회 이상 (2쿠션/3쿠션 모두 유효)
- 장애물 없이 3회 시도 → **3/3 성공률** 달성

### 핵심 기술
| 기술 | 설명 |
|------|------|
| **PyBullet 공 전용 시뮬레이션** | 720각도 × 3속도 = 2160개 후보를 헤드리스 PyBullet에서 직접 탐색 |
| **각도 우선순위 정렬** | 실측 기반 안전 각도(50-180°) 우선 선택으로 글랜싱 블로우 제거 |
| **2쿠션 우선 전략** | 궤적이 짧은 2쿠션을 우선 → 예측 정확도 향상 (필요 시 3쿠션 자동 전환) |
| **벽 뚫림 방지** | safe_approach_dist 동적 계산 → 도구가 벽을 뚫지 않는 안전 궤적 |
| **Damped Least Squares IK** | 5회 반복 수렴 + 관절한계/특이점/바디뚫림 검증 |
| **PD 토크 제어** | Kp=5000, Kd=200 부스트로 1.0 m/s 수평 타격 달성 |
| **ㄴ자 도구 수평 타격** | TOOL_VERTICAL_DROP=6cm + TOOL_HORIZONTAL_EXT=3cm 오프셋 자동 계산 |
| **자동 캘리브레이션** | 좌표 보정(Phase 1) + 물리 파라미터 최적화(Phase 2) 완전 자동 |

---

## 🏗️ 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│                  State Machine (FSM)                    │
│  ┌──────┐   ┌───────┐   ┌────────┐   ┌─────────┐      │
│  │ SCAN │──▶│ THINK │──▶│ STRIKE │──▶│ OBSERVE │──┐   │
│  └──────┘   └───────┘   └────────┘   └─────────┘  │   │
│      ▲                                      │      │   │
│      └──────── (retry if miss) ─────────────┘      │   │
│      └──────── (next round if success) ────────────┘   │
└────────────┬───────────────┬──────────────┬────────────┘
             │               │              │
     ┌───────▼───────┐ ┌────▼────┐  ┌──────▼──────┐
     │ CushionPlanner│ │Traj.Plan│  │ RobotControl│
     │ (공 시뮬)     │ │(SE3 궤적)│  │ (PD 토크)   │
     └───────────────┘ └─────────┘  └─────────────┘
             │                              │
     ┌───────▼───────┐              ┌──────▼──────┐
     │ PyBullet      │              │ Pinocchio   │
     │ (Headless)    │              │ (FK, Jac)   │
     └───────────────┘              └─────────────┘
```

### 작동 흐름

1. **SCAN**: PyBullet에서 3공 위치 + 테이블 bounds 읽기 (실환경: 카메라 + detect_balls)
2. **THINK**: 720각도 × 3속도 공 전용 시뮬레이션 → 안전 각도 + 2쿠션 우선 정렬 → 최대 25개 후보
3. **STRIKE**: 후보별 IK 검증 → 첫 유효 후보 실행 → 즉시 Home 복귀
4. **OBSERVE**: 접촉 이벤트 추적 → 총 쿠션 ≥ 2 + 양 타겟 히트 → 성공

---

## 📁 디렉토리 구조

```
code/
├── project/                         # 핵심 프로젝트 코드
│   ├── config.py                    # 전체 설정 파라미터
│   ├── robot_controller.py          # 로봇 제어기 (GUI/Headless/Real)
│   ├── state_machine.py             # 자율 상태 머신 (FSM)
│   ├── trajectory_planner.py        # ㄴ자 도구 수평 타격 궤적 생성
│   ├── ik_solver.py                 # DLS IK 솔버 + 궤적 검증
│   ├── perception.py                # 환경 인식
│   ├── real_env_to_pybullet.py      # 실환경→PyBullet 좌표 변환
│   │
│   ├── environment/
│   │   └── maze_env.py              # 3쿠션 테이블 + 3공 + 도구
│   │
│   └── physics/
│       ├── cushion_planner.py       # 공 전용 시뮬 + 각도 우선순위 탐색
│       └── cushion_rules.py         # 접촉 이벤트 추적기
│
├── run_maze.py                      # ★ 메인 실행 파일
├── calibration_loop.py              # 완전 자동 2단계 캘리브레이션
├── 6TestCode_RealRobot.py           # 실로봇 단일 타격 테스트
├── 7TestCode.py                     # 실로봇 시뮬-재생 통합 테스트
├── src/                             # 프레임워크 (수업 제공)
└── calibration_result.npz           # 카메라 캘리브레이션
```

---

## 🔍 핵심 모듈

### 1. `cushion_planner.py` — 공 전용 시뮬 기반 탐색

```
1. Grid Search: 720각도(0.5°) × 3속도 = 2160개 후보
2. PyBullet 헤드리스에서 3공 직접 시뮬레이션
3. 각도 우선순위 정렬 (글랜싱 블로우 방지):
   - 50-180°: 안전 (우선순위 0) ← 항상 정확한 타격
   - 30-50°, 180-330°: 보통 (우선순위 1)
   - 0-30°, 330-360°: 위험 (우선순위 2) ← 글랜싱 블로우 빈발
4. 같은 우선순위 내에서 쿠션 수 적은 것 우선 (2쿠션 > 3쿠션)
5. 벽 뚫림 방지: safe_approach_dist 동적 계산
6. 5° 각도 다양성 필터 → 최대 25개 후보
```

### 2. `state_machine.py` — 자율 상태 머신

**성공 기준**: 양 타겟 히트 + 전체 이벤트에서 총 쿠션 ≥ 2 (2쿠션/3쿠션 모두 유효)

**후보 선택**: IK 검증 통과하는 첫 후보 실행
- Manipulability ≥ 0.015 (글랜싱 블로우 방지)
- 바디 뚫림 z < 0.30m
- 관절 점프 < 0.3 rad
- 후보별 safe_approach_dist 적용 (벽 안전)

### 3. `trajectory_planner.py` — ㄴ자 도구 수평 타격

```
ㄴ자 도구 형상:
  EE
   |  (6cm 수직)
   |
   └──● (3cm 수평, 큐팁 직경 13mm)

궤적: Home → 상공(25cm) → Ready(공 뒤 8~10cm) → Impact(공) → Follow(5cm, bounds 클램프) → Home
```

### 4. `calibration_loop.py` — 완전 자동 캘리브레이션

```
Phase 1 (좌표): 카메라 검출 → 로봇 접근 → 접촉 여부로 오프셋 자동 보정
Phase 2 (물리): 자동 타격 → 카메라 관측 → Nelder-Mead로 6개 파라미터 최적화
               (반발계수, 마찰, 속도전달비, 테이블 위치 보정)
```

### 5. `ik_solver.py` — 역기구학 솔버 + 손목 특이점 회피

- 5회 반복 DLS IK 수렴 (1회만 하면 곡선 궤적 발생)
- 궤적 전체에서 검증 (`validate_from` 이후 모든 포인트)
- **6단계 검증**: 관절한계, 바디뚫림, 관절점프, **도구팁 범위**, **J3·J5 특이점**, manipulability

#### 손목 특이점 회피 (신규)

**문제**: Indy7에서 joint3(전완 회전)과 joint5(EE 롤)의 회전축이 평행해지면 DLS IK가 발산하고 movel이 실패합니다. 이 조건은 joint4(손목 굽힘)가 축을 정렬시키는 특정 값에 있을 때 발생합니다.

**감지 방법**: 관절 인덱스(J4=0°)가 아닌 **FK 기반 실제 축 외적**으로 감지합니다.
```python
# Pinocchio FK로 joint3, joint5의 월드 회전축 추출
j3_axis = oMi[4].rotation @ [0, 0, 1]   # joint3 월드 축
j5_axis = oMi[6].rotation @ [0, 0, 1]   # joint5 월드 축
cross_mag = ||cross(j3_axis, j5_axis)||  # 0이면 축 평행 → 특이점
```
URDF joint origin에 프리셋 회전(rpy)이 있어서 관절값 0°가 곧 특이점이 아닙니다. FK 기반 외적이 유일하게 정확한 방법입니다.

**회피 방법**: `solve_step()` 매 IK 스텝에서 cross_mag < 0.15이면 **q[4](joint4, 꺾임 관절)에 0.10 rad(5.7°) perturbation**을 가합니다. joint4가 J3-J5 사이의 꺾임을 담당하므로, 이걸 살짝 밀면 두 축이 분리됩니다.

```python
if cross_mag < 0.15:
    push_dir = sign(q[4])  # 현재 방향으로 밀어서 자연스럽게 벗어남
    q[4] += push_dir * 0.10
```

**검증 결과**: 7위치 × 8방향 = 56개 테스트에서 IK 성공률 76.8% → **98.2%** 달성.

| 변경 | perturbation | 성공률 |
|------|-------------|--------|
| 수정 전 (J4 인덱스 기반) | q[3], 0.03 rad | 76.8% |
| 수정 후 (FK 외적 기반) | q[4], 0.10 rad | **98.2%** |

#### 도구 팁 범위 체크 (신규)

EE 위치가 아닌 **ㄴ자 도구 팁의 실제 위치**가 테이블 쿠션 내부에 있는지 검사합니다.
```python
# SE3 목표에서 도구 팁 위치 계산
tool_tip = ee_pos + R_ee @ [TOOL_HORIZONTAL_EXT, 0, -TOOL_VERTICAL_DROP]
# 이 팁이 table_bounds 안에 있는지 검사 (5mm 마진)
```

#### `solve_trajectory_validated()` 반환값

```python
{
    'q_trajectory': [...],           # IK 결과 궤적
    'valid': bool,                   # 모든 검증 통과 여부
    'issues': [str, ...],            # 실패 원인 상세 (진단용)
    'min_manipulability': float,     # 최소 manipulability
    'min_singularity_margin': float, # J3·J5 외적 최소값 (0=특이점)
    'joint_limit_violations': [...],
}
```

### 6. `state_machine.py` — 최종 후보 검증 및 실행 (saveState)

단순히 물리 엔진(Headless)에서 점수가 높은 궤적을 맹신하지 않고, **GUI 환경에서 실제 타격을 시뮬레이션하여 검증**합니다.
- `cushion_planner.py`에서 찾은 상위 5개 후보 궤적을 수집합니다.
- 각 후보에 대해 `pybullet.saveState()`로 현재 상태를 저장합니다.
- 로봇 도구가 실제로 공을 치는 궤적을 실행하고 결과를 확인합니다.
- **성공 시**: 상태를 복원하지 않고 그대로 다음 단계를 진행합니다. (1순위에서 바로 성공하면 시간 지연 없음)
- **실패 시**: `restoreState()`로 공과 로봇을 원위치시킨 후 다음 순위 후보를 테스트합니다.
- 이 과정을 통해 시뮬레이션 계산 오차나 예상치 못한 도구 간섭(샌드위치 효과)을 극복합니다.

---

## 🚀 설치 및 실행

### 요구 사항
```
Python 3.10+, PyBullet, NumPy, SciPy, Matplotlib, Pinocchio
```

### 실행
```bash
conda activate indy7_project
cd code/

# 3쿠션 당구 (장애물 없음, 3회 시도)
python run_maze.py --obstacles 0 --attempts 3

# 장애물 5개
python run_maze.py --obstacles 5 --attempts 3

# 자동 캘리브레이션 (실로봇 + 카메라)
python calibration_loop.py --phase all --num-trials 5
```

---

## 📊 성능 및 최적화

### 달성 성능 (10-Test Stress Test)
| 지표 | 값 |
|------|-----|
| **성공률** | **9/10 (90%)** — 무작위 배치 10회 테스트 |
| **타격 각도 정확도** | 0.0-0.3° 이내 오차 |
| **공 속도** | 1.82-1.88 m/s (일관적) |
| **글랜싱 블로우** | 0% (Score 최우선 정렬 및 saveState 검증) |
| **검증 시간** | 실패 후보당 ~10초 (1순위 성공 시 지연 없음) |

### 최적화 히스토리
| 단계 | 문제 | 해결 | 성공률 |
|------|------|------|--------|
| **v1.0** | 3쿠션 궤적을 찾아도 로봇이 치면 빗나감 | 각도/거리 우선 정렬 도입 | 20% |
| **v2.0** | 공을 비스듬히 치는 글랜싱 블로우 발생 | 타격 각도와 여유 공간(score)을 최우선으로 정렬 | 60% |
| **v3.0** | 도구가 공과 벽 사이에 끼이거나 미세 오차로 실패 | **상위 5개 후보 saveState 검증 도입**. 1순위가 실패해도 2~5순위 중 성공하는 타격을 찾아내어 실행 | **90%** |

### 세부 해결 기법
- **safe_approach_dist 동적 계산**: 벽에 가까우면 도구가 뚫고 들어가지 않도록 접근 거리를 동적 축소하고 패널티 부여.
- **Follow-through 클램프**: 벽 밖으로 나가는 목표 궤적은 테이블 경계 내로 클램핑하여 안전 보장.
- **IK 곡선 궤적 방지**: IK 5회 반복 수렴.
- **saveState 기반 실증**: Headless 시뮬레이션의 한계(도구 간섭 미반영)를 극복하기 위해, 상위 5개 후보를 GUI 환경에서 직접 쳐보고 성공하는 것만 최종 채택.

### 남은 한계
1. **극단적 샌드위치 (10% 실패 원인)**: 공이 벽에 5cm 이내로 극도로 밀착된 경우, 로봇 도구가 들어갈 물리적 공간이 없어 상위 5개 후보 모두 실패. (이는 인간 플레이어에게도 물리적으로 매우 어려운 배치에 해당함)
2. **Sim-to-Real Gap**: 캘리브레이션으로 완화하지만 완전 제거 불가.

---

## 🎱 왜 이 프로젝트는 어려운가

> **"위치 알면 공식대로 치면 되는 거 아닌가요?"**
>
> 이 프로젝트는 Pick-and-Place가 아닙니다.

### 근본적 차이: 정적 배치 vs 동적 충격

| 비교 항목 | Pick & Place | 당구 타격 |
|-----------|-------------|-----------|
| **제어 대상** | 위치 (x, y, z) | 위치 + 속도 + 방향 + 타이밍 |
| **동작 속도** | 느림 (~0.1 m/s) | 빠름 (**1.0+ m/s**) |
| **접촉 성격** | 정적 파지 (grasp) | **충격 (impulse, <1ms)** |
| **오차 전파** | 없음 (놓으면 끝) | **기하급수적 증폭** |
| **물리 모델** | 불필요 | 반발계수, 마찰, 스핀 전부 필요 |
| **계획 복잡도** | Point-to-point | **조합 탐색 (720각도 × 물리 시뮬)** |
| **되돌릴 수 있나?** | 즉시 재배치 | 불가능 (공은 이미 굴러감) |

Pick-and-place에서 1mm 오차는 그대로 1mm입니다.
당구에서 **0.5° 각도 오차**는 2쿠션 후 **수십 cm**의 궤적 차이로 증폭됩니다.

### "공식대로 하면 된다"가 틀린 5가지 이유

#### 1️⃣ 공식이 가정하는 이상적 조건은 현실에 없다

교과서 쿠션 반사 공식: **입사각 = 반사각**

현실:
- 쿠션 고무의 탄성은 **비선형** (입사 속도에 따라 반발계수가 변함)
- 공이 쿠션에 **파묻히며** 접촉 시간 동안 회전(스핀)이 변함
- 쿠션 높이가 공 중심보다 약간 높아서 **하향 스핀**이 생김
- 테이블 천 마찰은 **방향에 따라 다름** (결이 있음)
- 같은 테이블이라도 온도·습도에 따라 마찰이 달라짐

> 결론: 공식은 1쿠션까지는 근사적으로 맞지만, 2쿠션 이상에서는 **매번 다른 결과**가 나옵니다. 그래서 물리 시뮬레이터(PyBullet)로 직접 시뮬레이션 + 현실 데이터로 파라미터를 캘리브레이션해야 합니다.

#### 2️⃣ 로봇 팔은 당구 큐가 아니다

프로 당구 선수는 큐를 잡고 **시선 → 공 → 목표**를 일직선으로 맞춘 뒤 부드럽게 밀어칩니다.

로봇 팔은:
- **6개 관절의 회전 조합**으로 직선 운동을 만들어야 함 (역기구학)
- 타격 순간 **특이점(singularity)** 근처에서 관절 속도가 발산할 수 있음
- ㄴ자 도구 끝의 실제 궤적은 EE 궤적과 **3cm × 6cm 오프셋** 관계
- 관절 토크 한계 때문에 원하는 속도를 못 낼 수 있음
- PD 컨트롤러의 추종 오차: 1.0 m/s에서 **1.2~1.7mm** 위치 오차

> Pick-and-place는 속도가 느려서 PD 오차가 무시 가능. 당구 타격은 **충격 직전의 0.1mm가 결과를 바꿉니다**.

#### 3️⃣ "칠 수 있는 각도"가 생각보다 적다

이론적으로 360° 모든 방향으로 칠 수 있지만:

- **벽 근처 공**: 도구가 벽을 뚫을 수 없어 접근 불가 → 30~50%의 각도가 차단됨
- **로봇 작업 공간**: 테이블 좌측 끝(x<0.10m)은 로봇 팔이 닿지 않음
- **특이점 회피**: 팔이 완전히 펴진 자세(singularity)에서는 IK 불가
- **ㄴ자 도구 충돌**: 도구 자체가 테이블에 닿아서 접근 불가한 자세 존재
- **글랜싱 블로우**: 0~30° 범위에서 도구가 공을 빗겨쳐서 속도 전달 실패

실측 결과: 평균적으로 전체 후보 중 **15~25%만** IK+충돌+벽 검사를 통과합니다.

#### 4️⃣ 시뮬레이션과 현실의 괴리 (Sim-to-Real Gap)

시뮬레이터에서 완벽한 3쿠션 궤적을 찾아도:

| 요인 | 시뮬 | 현실 |
|------|------|------|
| 마찰계수 | 고정 (0.15) | 미지수 (0.1~0.3) |
| 반발계수 | 고정 (0.85) | 속도 의존적 |
| 공 스핀 | 미반영 | 항상 존재 |
| 카메라 오차 | 0mm | 2~5mm |
| 테이블 수평 | 완벽 | 미세 경사 있음 |
| 도구-공 접촉 | 점 접촉 | 면적 접촉 |

> 이런 괴리를 줄이기 위해 **자동 캘리브레이션 루프**(calibration_loop.py)를 구현했습니다. 로봇이 자동으로 타격→카메라 관측→파라미터 최적화를 반복합니다.

#### 5️⃣ 2쿠션 규칙의 조합 폭발

"수구로 황구와 적구를 모두 치되, 쿠션에 2회 이상 반사" 조건은:

- 수구 타격 각도: 720개 (0.5° 단위)
- 각 각도에서 쿠션 반사 경로: **비결정적** (공 스핀, 속도에 따라 변함)
- 황구-적구 접촉 순서: 다양한 조합 가능
- 이 모든 조건을 만족하는 해: 전체 탐색 공간의 **2~5%**

단순 공식으로 이 탐색을 해결할 수 없습니다. 그래서 **물리 시뮬레이터 기반 전수 탐색** + **안전 각도 우선순위** + **2쿠션 우선 전략**을 사용합니다.

### 정리: 왜 어려운가

```
Pick & Place:    위치 → 잡기 → 놓기  (3단계, 정적, 오차 비증폭)
당구 타격:       감지 → 탐색(720각도 × 물리시뮬) → IK검증 → 궤적생성
                 → 접근 → 1ms 충격 → 관측 → 재시도  (8단계, 동적, 오차 증폭)
```

이 프로젝트의 난이도는 **"위치를 아는 것"이 아니라 "정확히 치는 것"과 "친 뒤의 결과를 예측하는 것"**에 있습니다.

- 위치를 안다고 칠 수 있는 게 아닙니다 (IK 한계, 도구 충돌)
- 칠 수 있다고 원하는 대로 가는 게 아닙니다 (물리 불확실성, 오차 증폭)
- 한 번 굴러간 공을 되돌릴 수 없습니다 (비가역적 동적 시스템)

> **프로 당구 선수가 이 과제를 어렵다고 느끼는 이유와 로봇이 어려워하는 이유는 같습니다:**
> 물리법칙은 간단해 보이지만, 2쿠션 이상에서 오차가 기하급수적으로 증폭되기 때문입니다.

---

## 🔬 설계 결정에 대한 기술적 근거

### Q1. "3쿠션 한다고 했다가 2쿠션으로 바꾸면 뭐가 프로젝트냐"

**답변: 3쿠션→2쿠션 우선은 물리적 현실에 기반한 최적 전략입니다.**

3쿠션을 2쿠션으로 "낮춘" 것이 아닙니다. **시스템은 3쿠션과 2쿠션을 모두 탐색**하며, 그 중 예측 정확도가 높은 것을 우선 실행합니다.

실측 데이터가 이를 증명합니다:

| 쿠션 수 | 궤적 길이 | 반사 오차 누적 | 예측 정확도 |
|---------|-----------|---------------|------------|
| 1쿠션 | ~30cm | ±0.2° | 95%+ |
| 2쿠션 | ~50cm | ±0.5° | **80~90%** |
| 3쿠션 | ~80cm | ±2~5° | **40~60%** |

3쿠션이 "더 어렵기 때문에 더 가치 있다"는 것은 **인간 기준**입니다. 로봇 시스템에서는:

- 3쿠션 궤적은 쿠션 반사 시 마찰·스핀·비선형 반발이 **3회 누적**
- 시뮬레이터와 현실의 물리 차이가 **반사마다 증폭**
- 시뮬에서 완벽한 3쿠션도 현실에서는 **50% 이상 빗나감**

반면 2쿠션은:
- 궤적이 짧아 오차 누적이 적음
- **같은 규칙(수구→쿠션2회 이상→양 타겟 접촉)**을 만족
- 실제 3쿠션 경기에서도 프로 선수들은 **최소 쿠션 경로를 우선** 선택

> 프로 당구 선수가 5쿠션 가능한 상황에서 2쿠션으로 치는 것을 "쉬운 길을 선택했다"고 하지 않습니다.
> **최소 쿠션 경로 선택은 당구의 기본 전략**이며, 이를 구현한 것은 도메인 지식의 적용입니다.

실제로 우리 시스템은 2쿠션 후보가 없으면 **자동으로 3쿠션을 시도**합니다. 3쿠션 코드를 제거한 것이 아니라, **2쿠션이 가능할 때 우선 선택**할 뿐입니다. 시뮬레이션에서 2쿠션 경로가 없을 때 3쿠션(159.5°)으로 성공한 사례가 이를 증명합니다.

---

### Q2. "말은 많은데 구체화된 게 없다"

**답변: 아래 항목들은 모두 코드로 구현되어 동작하는 결과물입니다.**

"구체화"의 정의가 "작동하는 시스템"이라면, 다음을 확인해 주십시오: please

| 구현 항목 | 파일 | 코드 라인 | 실측 결과 |
|-----------|------|----------|-----------|
| 720각도 전수 탐색 | `cushion_planner.py` | ~700줄 | 3368개 후보 → 2.8초 |
| 글랜싱 블로우 발견·제거 | `cushion_planner.py` | `_angle_priority()` | 0-30° 위험 구간 실측 확인 |
| ㄴ자 도구 기구학 | `trajectory_planner.py` | `plan_strike()` | 6cm×3cm 오프셋 자동 보정 |
| IK 5회 수렴 + 4단계 검증 | `ik_solver.py` | `solve_trajectory()` | 특이점/관절한계/충돌/점프 |
| PD 토크 부스트 | `robot_controller.py` | Kp 800→5000 | 0.5→1.0 m/s 타격 속도 달성 |
| 벽 뚫림 방지 | `cushion_planner.py` | `safe_approach_dist` | 동적 접근 거리 계산 |
| 2쿠션 우선 전략 | `cushion_planner.py` | `_score_result()` | 성공률 66%→100% (3/3) |
| 자동 캘리브레이션 | `calibration_loop.py` | ~540줄 | 좌표+물리 2단계 자동 보정 |
| 실로봇 재생 | `7TestCode.py` | `replay_trajectory_on_real()` | 시뮬 궤적 실로봇 재현 |

이 항목들은 각각 **"왜 필요한지"를 실패 데이터에서 발견하고, 코드로 해결하고, 실측으로 검증한** 결과입니다:

1. PD 게인 800에서 타격 속도 0.5 m/s → 쿠션 도달 불가 → **Kp 5000으로 부스트해서 해결**
2. 각도 15°에서 공이 85° 빗나감 → 글랜싱 블로우 현상 발견 → **안전 각도 우선순위로 해결**
3. 3쿠션 예측이 현실과 40% 불일치 → **2쿠션 우선 전략으로 해결**
4. 도구가 벽을 뚫는 궤적 생성 → **safe_approach_dist 동적 계산으로 해결**

> 각 항목은 "말"이 아니라 **실패→원인분석→구현→검증의 엔지니어링 사이클**을 거친 결과입니다.

---

### Q3. "바닥에 하드보드지 까는 건 로봇이 쉽게 하려고 환경을 바꾸는 것"

**답변: 환경 캘리브레이션은 로보틱스의 표준 절차이며, 인간에게도 동일하게 적용됩니다.**

세 가지 관점에서 반박합니다:

**① 인간과 동일 조건입니다**

이 프로젝트는 **사람과 로봇이 같은 테이블에서 같은 규칙으로** 경쟁합니다. 하드보드지를 깔면 사람도 같은 표면 위에서 칩니다. 로봇에게만 유리한 변경이 아닙니다.

**② 결함 있는 장비 보정은 당연합니다**

| 교실 당구대 현실 | 문제 |
|-----------------|------|
| 표면 거칠기 불균일 | 공이 직선으로 안 굴러감 → 물리 예측 자체가 불가능 |
| 수평 미보장 | 공이 한쪽으로 흘러감 → 정지 상태에서도 위치 변동 |
| 쿠션 탄성 불균일 | 같은 각도 같은 속도로 쳐도 반사각이 매번 다름 |

공이 **직선으로 굴러가지 않는 환경에서** 당구를 하는 것은 "어려운 도전"이 아니라 **물리적으로 불가능한 조건**입니다. 프로 당구 선수도 테이블 천이 훼손되면 교체합니다. 이것을 "환경을 바꾸는 것"이라 하지 않고 **"정상적인 경기 조건을 확보하는 것"**이라 합니다.

**③ 산업 로보틱스에서 환경 정비는 표준입니다**

- 자동차 공장: 차체 지그(jig) 정밀도 ±0.1mm로 관리 → "환경을 바꾸는 것"이 아니라 **공정 조건 확보**
- 반도체 팹: 클린룸 Class 100 유지 → 로봇이 쉽게 하려고가 아니라 **작업 가능 조건**
- 의료 로봇: 수술대 수평 보정 → **기본 셋업**

당구 테이블 표면 정비도 같은 맥락입니다. 물리 시뮬레이터가 가정하는 **"공이 직선으로 굴러감"**은 당구의 가장 기본적인 전제 조건이며, 이 전제가 깨진 환경에서는 **어떤 알고리즘도 작동할 수 없습니다**.

> 우리가 한 것은 **비정상적인 표면을 정상으로 복원**한 것이지, 난이도를 낮춘 것이 아닙니다.
> 사실 하드보드지 위에서도 마찰계수는 실제 당구대 천보다 높아서, 오히려 **공이 더 빨리 멈추고** 쿠션 도달이 더 어렵습니다.

---

## 🎱 포켓볼 데모 & POSTECH 트릭샷

3쿠션 당구와 별도로, **포켓볼 규칙 데모**(Phase 1)와 **POSTECH 글자 완성 트릭샷**(Phase 2)을 구현합니다.

### 개요

| Phase | 목표 | 설명 |
|-------|------|------|
| **Phase 1** | 포켓볼 | 큐볼로 3개의 목적구를 하나씩 포켓에 넣음 |
| **Phase 2** | POSTECH 트릭샷 | "PC'S.TECH" 배치에서 큐볼 한 번 타격으로 두 공을 O 형태로 완성 |

### 실행 방법

```bash
# Phase 1: 포켓볼 데모 (기본)
python run_pocket_demo.py

# Phase 2: POSTECH 트릭샷
python run_pocket_demo.py --phase 2

# 주요 옵션
python run_pocket_demo.py --phase 1 --attempts 3 --view 30
#   --phase    : 1 (포켓볼) 또는 2 (트릭샷)
#   --attempts : 각 공당 최대 시도 횟수 (기본: 3)
#   --view     : 결과 확인 대기 시간(초) (기본: 15)
#   --friction : 테이블 마찰 계수 (기본: 0.17)
```

### 신규/변경 파일

```
run_pocket_demo.py              # [NEW] 엔트리포인트 — Phase 선택 및 CLI 인터페이스
project/physics/pocket_planner.py # [NEW] 포켓볼 궤적 플래너 + 트릭샷 탐색 엔진
project/state_machine.py         # [MOD] Phase 1/2 상태 머신 추가
project/environment/maze_env.py  # [MOD] 포켓 환경 + POSTECH O 배치 로직
project/robot_controller.py      # [MOD] 큐볼 포켓 방지 로직
project/config.py                # [MOD] 포켓볼/트릭샷 파라미터 추가
```

---

### Phase 1: 포켓볼 데모 — 상세

#### 원리

1. **공 배치**: 큐볼(흰) + 목적구 3개(노/빨/검)가 테이블 중심선(X=0.485)을 따라 배치
2. **상태 머신**: `SCAN → PLAN → STRIKE → OBSERVE` 순환
   - `SCAN`: 현재 공 위치 읽기 (시뮬레이션에서는 PyBullet 직접 읽기)
   - `PLAN`: 목적구→포켓 경로를 역산하여 큐볼 타격 각도/속도 계산
   - `STRIKE`: IK 기반 궤적 생성 → 로봇 실행
   - `OBSERVE`: 포켓 여부 판단 → 다음 공으로 전환 또는 재시도
3. **큐볼 포켓 방지**: 큐볼이 포켓에 빠지면 원래 위치로 즉시 복원

#### 궤적 플래너 (`pocket_planner.py`)

```
목적구 → 포켓 방향 벡터 계산
    ↓
큐볼 → 목적구 접촉점 역산 (목적구 뒤쪽)
    ↓
필요한 공 속도 계산 (거리 + 마찰 보정)
    ↓
공 속도 → 도구 속도 변환 (1D 탄성 충돌 모델)
    ↓
IK 궤적 생성 (접근 → 타격 → 후퇴)
```

핵심 수식:
- **도구→공 속도 변환**: `v_ball = v_tool × (1 + e) × m_tool / (m_tool + m_ball)`
  - `e`: 반발계수 (도구=0.85, 공=0.8)
  - `m_tool`: 도구 질량 (0.005kg), `m_ball`: 공 질량 (0.01kg)
- **마찰 감속**: `v_final² = v_initial² - 2μgd`
  - `μ`: 구름마찰 (0.017), `d`: 이동 거리

#### 포켓 선택 로직

6개 포켓(코너 4 + 사이드 2) 중 최적 포켓 선택 기준:
1. 목적구→포켓 경로에 장애물(다른 공) 없음
2. 큐볼→목적구 경로에 장애물 없음
3. 총 이동 거리 최소
4. 타격 각도가 로봇 IK 도달 가능 범위 내

---

### Phase 2: POSTECH 트릭샷 — 상세

#### 컨셉

테이블 위에 "PC'S.TECH" 형태로 공을 배치하고, **큐볼 한 번 타격**으로 아포스트로피(') 위치의 두 공을 C 형태 갭으로 밀어 **O**를 완성합니다.

```
읽기 방향 (Y+) →

    [C형 4공]   [' 2공]  [. 큐볼]
        ↓           ↓         ↓
      P O S T E C H
        ↑
    트릭샷으로 C→O 완성
```

#### 배치 구조 (`maze_env.py::setup_postech_o`)

| 요소 | 위치 | 설명 |
|------|------|------|
| **O 타원** | 중심 (CX, CY-0.12) | rx=0.035m, ry=0.045m |
| **C형 4공** | 0°/60°/270°/300° | 타원 위 X+/Y+ 쪽에 배치 |
| **O 갭** | 150°/210° | X- 쪽 열림 (아포스트로피 방향) |
| **Trick balls** | (CX-0.04, apos_y ± gap) | 아포스트로피 위치, Y방향 세로 배치 |
| **Cue ball** | (CX+0.04, apos_y+0.06) | 마침표 위치, X+ 오프셋 |

#### 트릭샷 탐색 엔진 (`pocket_planner.py::plan_trick_shot`)

**2단계 탐색** 방식:

```
Stage 1 (Coarse): 360° × 17 speeds = 6,120 시뮬레이션
    → 1° 단위, 속도 0.3~2.0 m/s (0.1 간격)
    → 상위 5개 후보 선별
        ↓
Stage 2 (Fine): 상위 후보 근처 ±5° × ±0.2 m/s
    → 0.3° 단위, 속도 0.02 간격
    → ~700 추가 시뮬레이션/후보
        ↓
최적 각도/속도 결정 (총 ~8,000 시뮬)
```

각 시뮬레이션은 **headless PyBullet** (GUI 없이)에서 실행:
1. 테이블/쿠션/포켓 동일 환경 생성
2. 큐볼에 (angle, speed) 부여
3. 1500 스텝 시뮬 (6.25초)
4. 두 trick ball의 최종 위치 → 목표 거리 계산
5. 스코어링: 거리, C공 변위, 스크래치 여부 종합

#### 스코어링 기준

| 조건 | 점수 영향 |
|------|----------|
| 기본 | `300,000 × (1 - dist/0.3)` |
| 합산 거리 < 2cm | +500,000 보너스 |
| 합산 거리 < 4cm | +200,000 보너스 |
| 합산 거리 < 8cm | +50,000 보너스 |
| C공 1cm+ 변위 | 점수 75% 감점 |
| 큐볼 스크래치 | 점수 50% 감점 |
| Trick ball 이동 < 3cm | 점수 0 (트릭샷 아님) |

#### 왜 O 갭이 X- 쪽인가?

큐볼(X+)이 trick balls(X-)를 치면, 공은 자연스럽게 **X-** 방향으로 밀립니다.
O 갭이 X+ 쪽이면 공이 반대 방향으로 날아가야 하므로 물리적으로 불가능합니다.
갭을 X- 쪽(150°/210°)에 배치하면, 큐볼의 타격 방향과 목표 방향이 일치하여
자연스러운 트릭샷이 됩니다.

---

### 주요 파라미터 조정 가이드

#### `config.py` — 물리 파라미터

| 파라미터 | 기본값 | 설명 | 조정 시나리오 |
|----------|--------|------|---------------|
| `MAZE_BALL_FRICTION` | 0.17 | 표면 마찰 | 테이블 천 변경 시 |
| `MAZE_BALL_ROLLING_FRICTION` | 0.017 | 구름 마찰 | 공이 너무 멀리/가까이 갈 때 |
| `MAZE_BALL_RESTITUTION` | 0.8 | 공 반발계수 | 공-공 충돌 에너지 |
| `POCKET_DEMO_CUSHION_RESTITUTION` | 0.5 | 쿠션 반발 | 쿠션 바운스 세기 |
| `POCKET_RADIUS` | 0.0225 | 포켓 크기 | 포켓 난이도 조절 |
| `BALL_SPEED_GAIN` | ~1.73 | 도구→공 속도 | 로봇 타격이 너무 세/약할 때 |

#### `maze_env.py` — O 배치 파라미터

| 파라미터 | 위치 | 설명 |
|----------|------|------|
| `o_cx, o_cy` | 566행 | O 중심 좌표 |
| `rx, ry` | 567행 | O 타원 반경 (x, y) |
| `c_angles_deg` | 592행 | C형 4공의 각도 (0°/60°/270°/300°) |
| `target_angles_deg` | 593행 | 갭(목표) 각도 (150°/210°) |
| `apos_x` | 617행 | 아포스트로피 X 위치 (CX-0.04) |
| `apos_y` | 616행 | 아포스트로피 Y 위치 |

#### `pocket_planner.py` — 탐색 파라미터

| 파라미터 | 위치 | 설명 |
|----------|------|------|
| `speeds` | 876행 | Coarse 속도 범위 (0.3~2.0, 0.1간격) |
| `angles` | 877행 | Coarse 각도 범위 (360°, 1°간격) |
| Fine `±5°, 0.3°` | 970행 | Fine 각도 범위/해상도 |
| Fine `±0.2, 0.02` | 971행 | Fine 속도 범위/해상도 |
| `min_move = 0.03` | 934행 | Trick ball 최소 이동 거리 |

---

### 아키텍처 다이어그램

```
run_pocket_demo.py
    │
    ├── project/state_machine.py
    │       ├── run_phase1()  ← 포켓볼 자동화
    │       │      └── SCAN → PLAN → STRIKE → OBSERVE 루프
    │       │
    │       └── run_phase2_trickshot()  ← POSTECH 트릭샷
    │              └── plan_trick_shot() → IK → 타격
    │
    ├── project/physics/pocket_planner.py
    │       ├── plan_pocket_shot()     ← 포켓 경로 계산
    │       ├── plan_trick_shot()      ← 트릭샷 2단계 탐색
    │       ├── _create_pocket_env()   ← headless 시뮬 환경
    │       └── _make_ball() / _set_rolling_velocity()
    │
    ├── project/environment/maze_env.py
    │       ├── setup_pocket_balls()   ← Phase 1 공 배치
    │       ├── setup_postech_o()      ← Phase 2 O 배치
    │       └── _is_pocketed()         ← 포켓 판정
    │
    └── project/robot_controller.py
            └── cue_ball_pocket_check() ← 큐볼 복원
```

---

## 📝 라이선스

POSTECH 로보틱스개론 수업 프로젝트. 교육 목적으로만 사용.
